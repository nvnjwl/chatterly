{
  "name": "chatterly",
  "version": "1.0.0",
  "description": "chatterly",
  "main": "script.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cp -r ./src/* ./dist",
    "watch": "watch 'npm run build' ./src"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "http-server": "^14.1.1",
    "watch": "^1.0.2"
  }
}
